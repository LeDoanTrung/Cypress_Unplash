{
  "name": "cypress_framework_unplash",
  "version": "1.0.0",
  "main": "index.js",  "scripts": {
    "open": "cypress open",
    "start": "cypress run --browser chrome --headed",
    "test:qa": "set CYPRESS_ENV=qa && cypress run --browser chrome --headed",
    "test:staging": "set CYPRESS_ENV=staging && cypress run --browser chrome --headed",
    "test:prod": "set CYPRESS_ENV=production && cypress run --browser chrome --headed",
    "clean-reports": "rimraf cypress/reports/* && mkdir -p cypress/reports/html cypress/reports/json",
    "test:api-ci": "cypress run --spec \"cypress/e2e/test/API/*.cy.js\"",
    "test:api": "npm run clean-reports && cypress run --spec \"cypress/e2e/test/API/*.cy.js\"",
    "test:api-debug": "npm run clean-reports && cypress run --browser chrome --headed --spec \"cypress/e2e/test/API/*.cy.js\"",
    "test:ui": "npm run clean-reports && cypress run --env grepTags=@ui",
    "test:regression": "npm run clean-reports && cypress run --env grepTags=@regression",
    "test:smoke": "npm run clean-reports && cypress run --env grepTags=@smoke",
    "test:happyCase": "npm run clean-reports && cypress run --env grepTags=@happyCase",
    "merge-reports": "mochawesome-merge \"cypress/reports/json/*.json\" > cypress/reports/mochawesome.json",
    "generate-html-report": "marge cypress/reports/mochawesome.json -f report -o cypress/reports/html",
    "test-report": "npm run merge-reports && npm run generate-html-report",
    "test:api-with-report": "npm run clean-reports && npm run test:api && npm run test-report"
  },
  "keywords": [],
  "author": "Trung Le",
  "license": "ISC",
  "description": "This is Cypress Project for Automation Framework",
  "dependencies": {
    "@faker-js/faker": "^9.0.1",
    "ajv": "^8.17.1",
    "cypress-xpath": "^2.0.1",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "@cypress/grep": "^4.1.0",
    "cy2": "^4.0.9",
    "cypress": "^13.14.2",
    "cypress-mochawesome-reporter": "^3.8.2",
    "cypress-parallel": "^0.14.0",
    "del-cli": "^5.1.0",
    "marge": "1.0.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0",
    "rimraf": "^6.0.1",
    "serve": "^6.5.8"
  }
}
